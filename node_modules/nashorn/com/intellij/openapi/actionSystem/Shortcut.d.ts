import Object = require('nashorn/java/lang/Object');

declare class Shortcut extends Object {
	static EMPTY_ARRAY : Shortcut[];
	keyboard : boolean;
	isKeyboard() : boolean;
	startsWith(arg1 : Shortcut) : boolean;
}

export = Shortcut
